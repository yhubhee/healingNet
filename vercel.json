{
    "version": 2,
    "builds": [
        { "src": "app.js", "use": "@vercel/node" }
    ],
    "routes": [
        { "src": "/(.*)",
         "dest": "app.js",
         "method": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
         }
    ]
}