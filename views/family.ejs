<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Flip Card - HealingNet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        /* Flip Card Container */
        .flip-card {
            width: 300px;
            height: 400px;
            perspective: 1000px; /* Creates 3D space for the flip effect */
            margin: 2rem auto; /* Center the card */
        }

        /* Inner element that will rotate */
        .flip-card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            transition: transform 0.6s;
            transform-style: preserve-3d;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
        }

        /* Flip the card on hover */
        .flip-card:hover .flip-card-inner {
            transform: rotateY(180deg);
        }

        /* Front and Back of the card */
        .flip-card-front, .flip-card-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden; /* Hides the back side when not facing the viewer */
            border-radius: 0.5rem; /* Rounded corners to match Bootstrap cards */
            overflow: hidden; /* Ensure content stays within bounds */
        }

        /* Front Side */
        .flip-card-front {
            background-color: #fff; /* White background to match dashboard */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #333;
        }

        /* Back Side */
        .flip-card-back {
            background-color: #007bff; /* Bootstrap primary blue */
            color: #fff;
            transform: rotateY(180deg); /* Start rotated 180 degrees to face away */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 1.5rem;
            text-align: center;
        }

        /* Responsive adjustments */
        @media (max-width: 576px) {
            .flip-card {
                width: 250px;
                height: 350px;
            }
        }
    </style>
</head>
<body>
    <!-- Flip Card -->
    <div class="flip-card">
        <div class="flip-card-inner">
            <!-- Front Side -->
            <div class="flip-card-front">
                <h3 class="mb-3">Health Tips</h3>
                <i class="fas fa-heartbeat fa-3x text-primary"></i>
            </div>
            <!-- Back Side -->
            <div class="flip-card-back">
                <h4>Stay Healthy!</h4>
                <p>Drink plenty of water, eat balanced meals, and exercise regularly to maintain your well-being.</p>
                <a href="#" class="btn btn-light btn-sm mt-2">Learn More</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>